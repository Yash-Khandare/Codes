#include<stdio.h>
int int main()
{
	int arr[50] = {0}, size = 0;
	printf("Enter size of array": %d, &size);
	scanf("%d", &size);
	printf("arrays elememnts are:");

	for (int i = 0; i < size; i++){ // for loopp used for inputṇ
		scanf("%d", &arr[i]);  

			if(arr[i]>max){
				max = arr[i];
			}



	}
 
	printf("%d", &arr[i]);  



	return 0;
}