#include<stdio.h>
int int main()
{
	int arr[50] = {0}, size = 0;
	printf("Enter size of array": %d, &size);
	scanf("%d", &size);
	printf("arrays elememnts are:");

	for (int i = 0; i < size; i++){ // for loopp used for input
		scanf("%d", &arr[i]);  
	}

	for (int i = 0; i < size; i++){ // for loopp used for output
		printf("%d", &arr[i]);  
	}





	return 0;
}